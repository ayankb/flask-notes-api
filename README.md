# Notes REST API 

A simple REST API built with Flask and SQLite to manage notes.

## Features
- Create, Read, Update, Delete (CRUD)
- SQLite database
- JSON request/response
- Error handling

## Setup
```bash
pip install -r requirements.txt
python app.py
